apiVersion: v1
kind: Service
metadata:
  name: {{ include "lambda-system-prac-workflow.fullname" . }}
  labels:
    {{- include "lambda-system-prac-workflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lambda-system-prac-workflow.selectorLabels" . | nindent 4 }}
