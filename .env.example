# Database Configuration
# ⚠️  NEVER commit actual credentials. Copy this file to .env and fill with real values
# Usage: cp .env.example .env && edit .env with your actual secrets

POSTGRES_USER=deUser
POSTGRES_PWD=dePwd
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=deDbPreferablySameAsUser

